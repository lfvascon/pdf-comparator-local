# PDF Comparator Dependencies
# Install with uv (recommended): uv pip install -r requirements.txt
# Or with pip: pip install -r requirements.txt

# Core PDF Processing (replaces Poppler)
PyMuPDF>=1.23.0

# Image Processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Parallel Processing
joblib>=1.3.0

# Optional: PDF reading fallback
PyPDF2>=3.0.0
